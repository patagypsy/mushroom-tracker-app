import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.patagypsy.mushroomtracker',
  appName: 'Mushroom Tracker',
  webDir: 'www'
};

export default config;
